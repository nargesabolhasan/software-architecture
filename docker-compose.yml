services:
  backend:
    build: ./back-end
    container_name: node_backend
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build: ./my-next-app
    container_name: next_frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: unless-stopped
